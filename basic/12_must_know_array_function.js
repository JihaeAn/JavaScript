/**
 * Array Function
 */
let iveMembers = [
    '안유진',
    '가을',
    '레이',
    '장원영',
    '이서',
    '리즈'
];
console.log(iveMembers);

// push()
// push()는 반환값을 가지는데 배열의 길이를 반환해준다.
console.log(iveMembers.push('코드팩토리'));  // 7
console.log(iveMembers);
